// Kept in sync with package.json by a unit test (see __tests__/version.test.ts).
export const VERSION = "0.7.1";

export const USER_AGENT = `anilist-mcp-server/${VERSION} (+https://github.com/Grinv/anilist-mcp-server)`;
